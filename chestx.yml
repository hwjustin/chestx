name: chestx-test
channels:
  - conda-forge
  - anaconda
  - defaults
dependencies:
  - python=3.10
  - pip:
    - invoke
    - tqdm
    - torch==2.6.0
    - transformers==4.38.2
    - numpy==1.26.4
    - -e ./DeepSeek-VL2